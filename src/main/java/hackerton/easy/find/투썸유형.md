그래서 투썸(Two-sum) 류 문제의 공식 패턴이 등장함
패턴 1: 해시맵 O(n) 풀이 (가장 많이 쓰는 정답)
arr[i] + arr[j] = target
need= arr[j]라치면

템플릿 
need = target - currentValue
if map.containsKey(need):
return (map.get(need), index)
map.put(currentValue, index)


이 공식은 FiveGuys 햄버거 주문처럼 정해져 있음 ㅋㅋ
패턴 문제라서 그대로 외우면 됨.
